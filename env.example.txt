# Environment Variables for Vercel Deployment
# Copy these to Vercel Dashboard > Settings > Environment Variables

# Database (Required - RECOMMENDED: Neon for easiest Vercel integration)
# Option 1 - Neon (EASIEST): Vercel Dashboard > Storage > Create Database > Neon
# Option 2 - Supabase: Go to Settings > Database > Connection String  
# Option 3 - Prisma: Use your existing Prisma Postgres connection
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# NextAuth Configuration
NEXTAUTH_URL="https://your-app-name.vercel.app"  # Replace with your actual Vercel URL
NEXTAUTH_SECRET="your-nextauth-secret-here" # Generate with: openssl rand -base64 32

# JWT Configuration  
JWT_SECRET="your-jwt-secret-here" # Generate with: openssl rand -base64 32

# Optional: For local development
# NEXTAUTH_URL="http://localhost:3000"

# Note: Make sure your database allows connections from Vercel's IP ranges
# Supabase: Enable "Allow connections from anywhere" in Settings > Database
# Prisma: Ensure your database accepts external connections
